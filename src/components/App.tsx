import WordCounter from "./WordCounter";

function App() {
    return (
        <main className="bg-gradient-to-r from-blue-500 to-indigo-600 min-h-screen flex items-center justify-center">
            <WordCounter/>
        </main>
    );
}

export default App;